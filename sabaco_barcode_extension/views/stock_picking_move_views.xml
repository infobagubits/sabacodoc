<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Adiciona campo product_packaging_qty na lista de stock.move dentro do picking -->
        <record id="view_picking_move_tree_packaging_qty" model="ir.ui.view">
            <field name="name">stock.picking.move.list.packaging.qty</field>
            <field name="model">stock.move</field>
            <field name="inherit_id" ref="stock.view_picking_move_tree"/>
            <field name="arch" type="xml">
                <!-- Adiciona o campo product_packaging_qty após product_packaging_id -->
                <xpath expr="//field[@name='product_packaging_id']" position="after">
                    <field name="product_packaging_qty" 
                           string="Confezioni richieste" 
                           readonly="1" 
                           groups="product.group_stock_packaging"
                           optional="hide"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
